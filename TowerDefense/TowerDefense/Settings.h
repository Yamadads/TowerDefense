#pragma once

#include <glm\vec3.hpp>
#include <GLFW\glfw3.h>
#include <map>

const GLuint DefaultWidth = 800;
const GLuint DefaultHeight = 600;
const glm::vec3 CameraPosition = glm::vec3(5.0f, 5.0f, 0.0f);
const GLfloat GameSpeed = 60;
const glm::vec3 lightPosition = glm::vec3(0.0f, 2.0f, 0.0f);

